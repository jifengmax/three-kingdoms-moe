import type { GlobalOpts } from "../types.js";
type SkillPublishResult = {
    ok: true;
    status: "unchanged" | "would-publish" | "published";
    slug: string;
    displayName: string;
    folder: string;
    version: string;
    latestVersion: string | null;
    fileCount: number;
    fingerprint: string;
    versionId?: string;
};
export declare function cmdPublish(opts: GlobalOpts, folderArg: string, options: {
    slug?: string;
    name?: string;
    owner?: string;
    sourceOwner?: string;
    version?: string;
    changelog?: string;
    tags?: string;
    categories?: string;
    topics?: string;
    forkOf?: string;
    migrateOwner?: boolean;
    sourceRepo?: string;
    sourceCommit?: string;
    sourceRef?: string;
    sourcePath?: string;
    dryRun?: boolean;
    json?: boolean;
    quiet?: boolean;
}): Promise<SkillPublishResult>;
export declare function resolveDefaultOwnerHandle(registry: string, token: string): Promise<string>;
export declare function prepareSkillFilesForPublish(folder: string): Promise<{
    relPath: string;
    bytes: Uint8Array;
    contentType?: string;
}[]>;
export {};
